index.html이 아닌

웹서버를 index.php 확장자가 html파일을 php파일이다 인식하고 html로 출력.

확장자 html, php의 차이 
html은 파일을 한번 만들면(정적) 그때화면 그대로 
PHP는 index.html 안을 열어보고 php의 함수가 등장하면(동적) 계속 바낌

정수 integer

php는 print()를 사용하면 html에 표현이 가능하다.
(echo도 마찬가지)

Variable(변수)

php의 url을 통한

expr 은 값

statement는 값이 아니다 

문법을 구성하는 양대 축 

php get file list in directory 
 
 //0725 함수와 객체를 이용하는 방식과 차이점

Api 기본적인 제공하는 함수.

//캡슐화 : 불필요한 정보를 숨기는 기능 
//예를들어 아이폰의 디자인의 내부(배터및 트랜지스터 보이는 상태) 외부 디자인 
// access modifier 객체를 사용하는 입장에서 객체를 사용하는 메소드에 
// 접근을 결정한다. (접근 제어자 의 설명)
// 접근 제어자랑 캡슐화랑 잘 기억하기. 
